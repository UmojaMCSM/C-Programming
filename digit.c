//Find the output of the following program
#include <stdio.h>
int main()
{
    int a, b;
    printf("\n Enter two four digit numbers: ");
    scanf("%2d %4d, &a, &b);
    printf("\n The two numbers are: %d adn %d", a, b);
    return 0;
}
